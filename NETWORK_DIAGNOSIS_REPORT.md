# 网络访问诊断报告

> 山东省政府采购网访问问题分析

**诊断时间：** 2026-02-13 18:35
**测试环境：** Linux 容器 (Los Angeles, US)
**目标网站：** http://www.ccgp-shandong.gov.cn/

---

## 一、问题现象

### ❌ 失败环境

| 环境 | 系统 | 位置 | 结果 |
|------|------|------|------|
| 容器 | Linux (Debian 12) | 🇺🇸 美国洛杉矶 | ❌ 连接 10 秒后被重置 |
| 用户本地 | Linux | 🇨🇳 中国大陆 | ❌ 无法访问 |
| agent-browser | Chromium 144 | 🇺🇸 美国 | ❌ 超时 |

### ✅ 成功环境

| 环境 | 系统 | 位置 | 结果 |
|------|------|------|------|
| 用户本地 | **Windows** | 🇨🇳 中国大陆 | ✅ 可以正常访问 |

---

## 二、测试过程

### 2.1 DNS 解析测试

```bash
$ nslookup www.ccgp-shandong.gov.cn
```

**结果：**
```
✅ 成功解析
IP: 119.148.160.205
```

### 2.2 TCP 连接测试

```bash
$ curl -v http://www.ccgp-shandong.gov.cn/
```

**结果：**
```
✅ TCP 连接成功 (端口 80)
✅ HTTP 请求发送
❌ 等待 10 秒后：Recv failure: Connection reset by peer
```

**详细输出：**
```
*   Trying 119.148.160.205:80...
* Connected to www.ccgp-shandong.gov.cn (119.148.160.205) port 80 (#0)
> GET / HTTP/1.1
> Host: www.ccgp-shandong.gov.cn
> User-Agent: curl/7.88.1
> Accept: */*
>
[等待 10 秒...]
* Recv failure: Connection reset by peer
curl: (56) Recv failure: Connection reset by peer
```

### 2.3 容器 IP 检测

```bash
$ curl -s https://ipinfo.io/json
```

**结果：**
```json
{
  "ip": "107.173.223.214",
  "hostname": "107-173-223-214-host.colocrossing.com",
  "city": "Los Angeles",
  "region": "California",
  "country": "US",
  "loc": "34.0522,-118.2437",
  "org": "AS36352 HostPapa",
  "postal": "90012",
  "timezone": "America/Los_Angeles"
}
```

🔍 **关键发现：** 容器出口 IP 在美国洛杉矶

### 2.4 不同 URL 测试

```bash
# HTTP
$ timeout 12 curl -I http://www.ccgp-shandong.gov.cn/
HTTP Code: 000  # 失败

# HTTPS
$ timeout 12 curl -I https://www.ccgp-shandong.gov.cn/
HTTP Code: 000  # 失败

# 无 www
$ timeout 12 curl -I http://ccgp-shandong.gov.cn/
HTTP Code: 000  # 失败

# IP 直连
$ timeout 12 curl -I http://119.148.160.205/
HTTP Code: 000  # 失败

# 对照组：百度
$ curl -I http://www.baidu.com/
HTTP Code: 200  # ✅ 成功
```

### 2.5 浏览器测试

```bash
# agent-browser (Chromium 144)
$ agent-browser open "http://www.ccgp-shandong.gov.cn/"
Result: chrome-error://chromewebdata/  # ❌ 失败
```

---

## 三、根本原因分析

### 3.1 为什么 GitHub 可以访问？

| 网站 | 类型 | 访问策略 | 结果 |
|------|------|----------|------|
| GitHub | 全球 CDN | 接受全球访问 | ✅ 成功 |
| 百度 | 中国网站 | 全球可访问 | ✅ 成功 |
| 山东采购网 | 政府网站 | **限制境外 IP** | ❌ 失败 |

### 3.2 为什么 Windows 可以，Linux 不行？

**可能的检测机制：**

1. **地理位置检测**
   - Windows 在中国大陆 → ✅ 通过
   - Linux 容器在美国 → ❌ 拒绝
   - Linux 本地（可能使用 VPN）→ ❌ 拒绝

2. **User-Agent 检测**
   - Windows + Chrome（真实浏览器）→ ✅ 通过
   - Linux + Chromium（自动化）→ ❌ 拒绝

3. **操作系统指纹**
   - Windows 系统特征 → ✅ 通过
   - Linux 系统特征 → ❌ 拒绝

4. **自动化检测**
   - 手动操作（鼠标轨迹、时间间隔）→ ✅ 通过
   - 自动化工具（固定模式）→ ❌ 拒绝

---

## 四、技术特征分析

### 4.1 服务器行为特征

```
1. DNS 解析正常 ✅
2. TCP 握手成功 ✅
3. HTTP 请求发送成功 ✅
4. 服务器保持沉默 10 秒
5. 主动发送 RST 断开连接 ❌
```

**这是典型的反爬虫/地域限制行为：**
- 不是简单的防火墙阻断（那样连 TCP 都建立不了）
- 不是 HTTP 403/401（那样会返回错误页面）
- 是应用层检测后的**主动拒绝**

### 4.2 可能的检测点

```
┌─────────────────┐
│   请求到达      │
│   服务器        │
└────────┬────────┘
         │
    ┌────▼─────┐
    │ 检查 IP  │──── 境外 IP? ───┐
    └────┬─────┘                 │
         │ 是中国 IP              │
    ┌────▼──────┐                │
    │ 检查 UA   │──── 自动化? ───┤
    └────┬──────┘                │
         │ 正常浏览器             │
    ┌────▼──────┐                │
    │ 检查系统  │──── Linux? ────┤
    └────┬──────┘                │
         │ Windows                │
    ┌────▼──────┐                │
    │  返回内容  │                │
    └───────────┘                │
                                 │
                            ┌────▼────┐
                            │ 等待10秒 │
                            │ 断开连接 │
                            └─────────┘
```

---

## 五、解决方案

### ✅ 推荐方案：Windows 本地部署

**为什么：**
- Windows 在中国大陆 IP → 通过地理位置检测
- 真实浏览器环境 → 通过自动化检测
- Go + Rod 可以精细控制 → 模拟人类行为

**实施：**
1. 在 Windows 上运行 Go 程序
2. 使用 Rod 控制真实 Chrome
3. 添加随机延迟、鼠标移动
4. 配置 Windows 定时任务

**参考：** `WINDOWS_DEPLOYMENT.md`

### ⚠️ 备选方案：中国大陆 VPS

**要求：**
- 必须是中国大陆 IP
- 最好使用 Windows Server
- 如果用 Linux，需要更强的反检测

### ❌ 不推荐：当前容器

**原因：**
- 美国 IP 被直接拒绝
- Linux 环境可能被检测
- 无法绕过地理位置限制

---

## 六、技术对策

### 6.1 如果必须使用 Linux

需要增强反检测能力：

```go
// 1. 使用 Stealth 插件
l := launcher.New().
    Set("disable-blink-features", "AutomationControlled").
    Set("disable-dev-shm-usage").
    Set("disable-background-timer-throttling").
    Set("disable-backgrounding-occluded-windows").
    Set("disable-renderer-backgrounding").
    Set("disable-background-networking")

// 2. 伪装成 Windows
l.Set("user-agent", "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36")

// 3. 执行 JavaScript 覆盖
page.MustEval(`() => {
    Object.defineProperty(navigator, 'webdriver', {get: () => false});
    Object.defineProperty(navigator, 'platform', {get: () => 'Win32'});
    Object.defineProperty(navigator, 'plugins', {get: () => [1, 2, 3, 4, 5]});
}`)

// 4. 添加随机延迟
time.Sleep(time.Duration(rand.Intn(2000)) * time.Millisecond)
```

### 6.2 中国代理方案

```go
l := launcher.New().
    Proxy("http://proxy.cn:8080")  // 使用中国代理
```

但仍可能被检测为代理流量。

---

## 七、测试结论

| 检测维度 | 容器环境 | 用户 Windows | 说明 |
|----------|----------|--------------|------|
| 地理位置 | ❌ 美国 | ✅ 中国 | 主要原因 |
| 操作系统 | ❌ Linux | ✅ Windows | 次要原因 |
| 浏览器 | ❌ Chromium | ✅ Chrome | 影响较小 |
| 自动化特征 | ❌ 明显 | ✅ 隐蔽 | 需要优化 |

---

## 八、建议行动

### 立即行动

1. ✅ **在 Windows 上部署**（推荐）
   - 使用现有的 Go 代码
   - 参考 `WINDOWS_DEPLOYMENT.md`
   - 配置定时任务自动运行

2. ⚙️ **优化 Rod 参数**
   - 增加随机延迟
   - 模拟鼠标移动
   - 伪装 User-Agent

3. 📝 **记录访问日志**
   - 监控失败率
   - 分析触发条件
   - 调整策略

### 长期计划

1. **多省份支持**
   - 录制其他省份的 trace
   - 测试不同网站的限制
   - 建立站点特征库

2. **智能切换**
   - 检测访问失败
   - 自动切换策略
   - 使用备用方案

3. **合规采集**
   - 遵守 robots.txt
   - 控制访问频率
   - 添加请求标识

---

## 九、参考资料

### 测试命令

```bash
# 基础测试
curl -v http://www.ccgp-shandong.gov.cn/

# 带 headers
curl -v -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" \
  -H "Accept-Language: zh-CN" \
  http://www.ccgp-shandong.gov.cn/

# 检查 IP
curl https://ipinfo.io/json

# 测试代理
curl -x proxy:port http://www.ccgp-shandong.gov.cn/
```

### 相关文档

- Rod 文档: https://go-rod.github.io/
- Stealth 插件: https://github.com/puppeteer/puppeteer-extra/tree/master/packages/puppeteer-extra-plugin-stealth
- ddddocr: https://github.com/sml2h3/ddddocr

---

*诊断日期：2026-02-13*
*诊断工具：curl, agent-browser, ipinfo*
*结论：地理位置限制 + 操作系统检测 + 自动化检测*
